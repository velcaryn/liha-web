# Agent instructions

See `CLAUDE.md`. It applies to every agent and every editor, not just Claude.

This file deliberately does not restate the rules. A second copy drifts from
the first within a release, and then two agents follow two different rulebooks
while both believe they are correct.
